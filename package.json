{
  "name": "ticketing-system",
  "version": "1.0.0",
  "description": "ticketing system for educational proppose",
  "license": "MIT",
  "author": "Matus behun",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=prod node server.js",
    "dev": "cross-env NODE_ENV=dev node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "optionalDependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "morgan": "^1.10.1"
  }
}
